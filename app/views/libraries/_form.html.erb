<%= form_with(model: library) do |form| %>
  <% if library.errors.any? %>
    <div class="text-red-500">
      <h2><%= pluralize(library.errors.count, "error") %> prohibited this library from being saved:</h2>

      <ul>
        <% library.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :library_name, class: "block text-gray-700" %>
    <%= form.text_field :library_name, class: "border rounded py-2 px-3 focus:outline-none focus:shadow-outline" %>
  </div>


  <div>
    <%= form.submit "Submit", class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" %>
  </div>
<% end %>

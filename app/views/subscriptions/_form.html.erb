<%= form_with(model: subscription) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.collection_select :user_id, User.order(:email), :id, :email, { include_blank: 'Select User' } %>
  </div>

  <div>
    <%= form.collection_select :app_id, App.order(:app_name), :id, :app_name, { include_blank: 'Select App' } %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<div id="<%= dom_id subscription %>">
  <p>
    <strong>User:</strong>
    <%= User.find(subscription.user_id).email %>
  </p>

  <p>
    <strong>App:</strong>
    <%= App.find(subscription.app_id).app_name %>
  </p>

    <p>
    <strong>Plan:</strong>
    <%= subscription.plan %>
  </p>

</div>
